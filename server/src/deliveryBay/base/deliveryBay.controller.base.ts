/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { DeliveryBayService } from "../deliveryBay.service";
import { DeliveryBayCreateInput } from "./DeliveryBayCreateInput";
import { DeliveryBayWhereInput } from "./DeliveryBayWhereInput";
import { DeliveryBayWhereUniqueInput } from "./DeliveryBayWhereUniqueInput";
import { DeliveryBayFindManyArgs } from "./DeliveryBayFindManyArgs";
import { DeliveryBayUpdateInput } from "./DeliveryBayUpdateInput";
import { DeliveryBay } from "./DeliveryBay";
import { BlockerFindManyArgs } from "../../blocker/base/BlockerFindManyArgs";
import { Blocker } from "../../blocker/base/Blocker";
import { BlockerWhereUniqueInput } from "../../blocker/base/BlockerWhereUniqueInput";
@swagger.ApiBearerAuth()
export class DeliveryBayControllerBase {
  constructor(
    protected readonly service: DeliveryBayService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "DeliveryBay",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: DeliveryBay })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: DeliveryBayCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<DeliveryBay> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "DeliveryBay",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"DeliveryBay"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: {
        ...data,

        dealership: {
          connect: data.dealership,
        },
      },
      select: {
        businessHours: true,
        createdAt: true,

        dealership: {
          select: {
            id: true,
          },
        },

        id: true,
        name: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "DeliveryBay",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: [DeliveryBay] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(DeliveryBayFindManyArgs)
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<DeliveryBay[]> {
    const args = plainToClass(DeliveryBayFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "DeliveryBay",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        businessHours: true,
        createdAt: true,

        dealership: {
          select: {
            id: true,
          },
        },

        id: true,
        name: true,
        updatedAt: true,
      },
    });
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "DeliveryBay",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: DeliveryBay })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: DeliveryBayWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<DeliveryBay | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "DeliveryBay",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        businessHours: true,
        createdAt: true,

        dealership: {
          select: {
            id: true,
          },
        },

        id: true,
        name: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "DeliveryBay",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: DeliveryBay })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: DeliveryBayWhereUniqueInput,
    @common.Body()
    data: DeliveryBayUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<DeliveryBay | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "DeliveryBay",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"DeliveryBay"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          dealership: {
            connect: data.dealership,
          },
        },
        select: {
          businessHours: true,
          createdAt: true,

          dealership: {
            select: {
              id: true,
            },
          },

          id: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "DeliveryBay",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: DeliveryBay })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: DeliveryBayWhereUniqueInput
  ): Promise<DeliveryBay | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          businessHours: true,
          createdAt: true,

          dealership: {
            select: {
              id: true,
            },
          },

          id: true,
          name: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id/bayBlocker")
  @nestAccessControl.UseRoles({
    resource: "DeliveryBay",
    action: "read",
    possession: "any",
  })
  @ApiNestedQuery(BlockerFindManyArgs)
  async findManyBayBlocker(
    @common.Req() request: Request,
    @common.Param() params: DeliveryBayWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Blocker[]> {
    const query = plainToClass(BlockerFindManyArgs, request.query);
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Blocker",
    });
    const results = await this.service.findBayBlocker(params.id, {
      ...query,
      select: {
        createdAt: true,
        blocker_dealershp: true,
        deliveryBay: true,
        id: true,
        owner: true,
        status: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post("/:id/bayBlocker")
  @nestAccessControl.UseRoles({
    resource: "DeliveryBay",
    action: "update",
    possession: "any",
  })
  async createBayBlocker(
    @common.Param() params: DeliveryBayWhereUniqueInput,
    @common.Body() body: DeliveryBayWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      bayBlocker: {
        connect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "DeliveryBay",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"DeliveryBay"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id/bayBlocker")
  @nestAccessControl.UseRoles({
    resource: "DeliveryBay",
    action: "update",
    possession: "any",
  })
  async updateBayBlocker(
    @common.Param() params: DeliveryBayWhereUniqueInput,
    @common.Body() body: BlockerWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      bayBlocker: {
        set: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "DeliveryBay",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"DeliveryBay"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id/bayBlocker")
  @nestAccessControl.UseRoles({
    resource: "DeliveryBay",
    action: "update",
    possession: "any",
  })
  async deleteBayBlocker(
    @common.Param() params: DeliveryBayWhereUniqueInput,
    @common.Body() body: DeliveryBayWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      bayBlocker: {
        disconnect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "DeliveryBay",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"DeliveryBay"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
