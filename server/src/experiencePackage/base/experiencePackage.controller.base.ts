/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/docs/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestMorgan from "nest-morgan";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { ExperiencePackageService } from "../experiencePackage.service";
import { ExperiencePackageCreateInput } from "./ExperiencePackageCreateInput";
import { ExperiencePackageWhereInput } from "./ExperiencePackageWhereInput";
import { ExperiencePackageWhereUniqueInput } from "./ExperiencePackageWhereUniqueInput";
import { ExperiencePackageFindManyArgs } from "./ExperiencePackageFindManyArgs";
import { ExperiencePackageUpdateInput } from "./ExperiencePackageUpdateInput";
import { ExperiencePackage } from "./ExperiencePackage";
import { DealershipFindManyArgs } from "../../dealership/base/DealershipFindManyArgs";
import { Dealership } from "../../dealership/base/Dealership";
import { DealershipWhereUniqueInput } from "../../dealership/base/DealershipWhereUniqueInput";
@swagger.ApiBearerAuth()
export class ExperiencePackageControllerBase {
  constructor(
    protected readonly service: ExperiencePackageService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "ExperiencePackage",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: ExperiencePackage })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: ExperiencePackageCreateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<ExperiencePackage> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "ExperiencePackage",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"ExperiencePackage"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: data,
      select: {
        active: true,
        chargeable: true,
        createdAt: true,
        duration: true,
        id: true,
        imageGallery: true,
        keyImage: true,
        name: true,
        price: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "ExperiencePackage",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: [ExperiencePackage] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(ExperiencePackageFindManyArgs)
  async findMany(
    @common.Req() request: Request,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<ExperiencePackage[]> {
    const args = plainToClass(ExperiencePackageFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "ExperiencePackage",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        active: true,
        chargeable: true,
        createdAt: true,
        duration: true,
        id: true,
        imageGallery: true,
        keyImage: true,
        name: true,
        price: true,
        updatedAt: true,
      },
    });
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "ExperiencePackage",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: ExperiencePackage })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: ExperiencePackageWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<ExperiencePackage | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "ExperiencePackage",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        active: true,
        chargeable: true,
        createdAt: true,
        duration: true,
        id: true,
        imageGallery: true,
        keyImage: true,
        name: true,
        price: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "ExperiencePackage",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: ExperiencePackage })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: ExperiencePackageWhereUniqueInput,
    @common.Body()
    data: ExperiencePackageUpdateInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<ExperiencePackage | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "ExperiencePackage",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"ExperiencePackage"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          active: true,
          chargeable: true,
          createdAt: true,
          duration: true,
          id: true,
          imageGallery: true,
          keyImage: true,
          name: true,
          price: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "ExperiencePackage",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: ExperiencePackage })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: ExperiencePackageWhereUniqueInput
  ): Promise<ExperiencePackage | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          active: true,
          chargeable: true,
          createdAt: true,
          duration: true,
          id: true,
          imageGallery: true,
          keyImage: true,
          name: true,
          price: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Get("/:id/dealership")
  @nestAccessControl.UseRoles({
    resource: "ExperiencePackage",
    action: "read",
    possession: "any",
  })
  @ApiNestedQuery(DealershipFindManyArgs)
  async findManyDealership(
    @common.Req() request: Request,
    @common.Param() params: ExperiencePackageWhereUniqueInput,
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<Dealership[]> {
    const query = plainToClass(DealershipFindManyArgs, request.query);
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Dealership",
    });
    const results = await this.service.findDealership(params.id, {
      ...query,
      select: {
        address: true,
        createdAt: true,
        id: true,
        name: true,
        updatedAt: true,
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results.map((result) => permission.filter(result));
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Post("/:id/dealership")
  @nestAccessControl.UseRoles({
    resource: "ExperiencePackage",
    action: "update",
    possession: "any",
  })
  async createDealership(
    @common.Param() params: ExperiencePackageWhereUniqueInput,
    @common.Body() body: ExperiencePackageWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      dealership: {
        connect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "ExperiencePackage",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"ExperiencePackage"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Patch("/:id/dealership")
  @nestAccessControl.UseRoles({
    resource: "ExperiencePackage",
    action: "update",
    possession: "any",
  })
  async updateDealership(
    @common.Param() params: ExperiencePackageWhereUniqueInput,
    @common.Body() body: DealershipWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      dealership: {
        set: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "ExperiencePackage",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"ExperiencePackage"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.UseInterceptors(nestMorgan.MorganInterceptor("combined"))
  @common.UseGuards(
    defaultAuthGuard.DefaultAuthGuard,
    nestAccessControl.ACGuard
  )
  @common.Delete("/:id/dealership")
  @nestAccessControl.UseRoles({
    resource: "ExperiencePackage",
    action: "update",
    possession: "any",
  })
  async deleteDealership(
    @common.Param() params: ExperiencePackageWhereUniqueInput,
    @common.Body() body: ExperiencePackageWhereUniqueInput[],
    @nestAccessControl.UserRoles() userRoles: string[]
  ): Promise<void> {
    const data = {
      dealership: {
        disconnect: body,
      },
    };
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "ExperiencePackage",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      throw new common.ForbiddenException(
        `Updating the relationship: ${
          invalidAttributes[0]
        } of ${"ExperiencePackage"} is forbidden for roles: ${roles}`
      );
    }
    await this.service.update({
      where: params,
      data,
      select: { id: true },
    });
  }
}
